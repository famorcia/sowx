Cookbook Procedure
==================

  1) set up and enter a build directory

    $ mkdir build && cd build

  2) run cmake

    $ cmake .. -DCMAKE_INSTALL_PREFIX=<your installation path /usr/local is the default>

  4) compile and install

    $ make install
